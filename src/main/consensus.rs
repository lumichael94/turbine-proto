extern crate rand;
extern crate crypto;
extern crate rustc_serialize;
extern crate postgres;
extern crate chrono;

use std::os;
use std::sync;

use net::{tcp, trusted};
use data::{account, state, database,};
use vm::env;
use util::{helper, krypto};

pub fn check_state(){

}

pub fn ask_state(){

}

pub fn trusted_state_hash(){

}

pub fn hash_state(){

}

pub fn execute_log(){

}
