mod node;

extern crate rand;
extern crate rust-crypto;
extern crate rustc-serialize;
extern crate postgres;
extern crate time;

use std::os
use std::sync

struct node {
    ip      :
    udp     :
    tcp     :
    pub_key :
    trusted ://list of trusted nodes
}
