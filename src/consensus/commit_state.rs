mod commit_state;

extern crate rand;
extern crate rust-crypto;
extern crate rustc-serialize;
extern crate postgres;
extern crate chrono;

use std::os
use std::sync

fn trusted_state(){

}

fn commit_state (){

}
